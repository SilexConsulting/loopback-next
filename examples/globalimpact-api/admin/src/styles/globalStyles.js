
import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles((theme) => ({
  app: {
    height: '100%',
    width: '100%',
    position: 'absolute',
  },
  show: {
    display: 'block',
  },
  hide: {
    display: 'none',
  },
  h100: {
    height: '100%',
  },
  w100: {
    width: '100%',
  },
}));

export default styles;
